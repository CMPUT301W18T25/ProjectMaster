<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mayingnan.project301">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".requester.RequesterAcceptedTaskDetail"
            android:label="@string/title_activity_requester_accepted_task_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".requester.RequesterFinishedTaskDetail" />
        <activity android:name=".provider.ProviderMapActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".requester.RequestTaskControl" />
        <activity android:name=".provider.ProviderTaskListActivity" />
        <activity android:name=".provider.ProviderBidHistoryActivity" />
        <activity android:name=".requester.RequesterMapActivity" />
        <activity android:name=".requester.RequesterEditTaskActivity" />
        <activity android:name=".provider.MakeABidActivity"></activity>
    </application>

</manifest>